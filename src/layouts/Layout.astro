---
import "@/styles/globals.css";
import Footer from "@/ui/footer";
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <meta name="description" content="Findata: Tu portal financiero completo con los mejores CDTs, cuentas de ahorro, indicadores económicos, simuladores y más." />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/x-icon" href="/favicon.ico" />

        <!-- Open Graph Meta -->
        <meta property="og:title" content={title} />
        <meta property="og:description" content="Findata: Tu portal financiero completo con los mejores CDTs, cuentas de ahorro, indicadores económicos, simuladores y más" />
        <meta property="og:image" content="/og-image.jpg" />

        <meta name="generator" content={Astro.generator} />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap"
            rel="stylesheet"
        />
        <title>{title}</title>
    </head>
    <body>
        <div class="w-full h-full p-5 flex justify-center">
            <div class="md:px-10 max-w-7xl w-full">
                <slot />
                <Footer client:load />
            </div>
        </div>
    </body>
</html>
